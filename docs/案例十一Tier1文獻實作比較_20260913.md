# 案例十一「有機會實作比較」第一梯隊 6 篇文獻——實作與比較結果（2026-09-13）

> 範圍：`docs/案例十一文獻延伸比較_20篇逐篇重點與本專案方法比較_20260913.md` 挑出的
> 第一梯隊 6 篇（資料現成、方法明確）中，實際完成 5 篇之重現實作與比較；第 6 篇
> （Geometric Distance Difference）因缺本機 MEME 精密星曆資料而暫緩，見第四節。
> 驗證基準：與本專案完全相同（`tasa14_compare.evaluate`，同真值、同 ±1.5 天配對容差、
> 同 1 天旗標合併），23 星標竿（14 原始標竿＋9 延伸衛星）。
> 可重現腳本：`lit_tier1_reproductions.py`；原始逐星結果：
> `data/benchmark/lit_tier1_persat_20260913.csv`。

## 一、結論摘要

**本專案方法（headline 規則式 F1=0.394、LOSO L3融合 F1=0.457-0.458）在同一驗證基準下，
優於全部 5 篇文獻重現實作**，其中表現最好的重現版（Mukundan & Wang 2021）F1=0.300，
仍落後本專案 headline 約 0.09、落後 L3 融合約 0.16。

| 方法 | 平均 Precision | 平均 Recall | 平均 F1 | 相對本專案 headline (F1=0.394) |
|---|--:|--:|--:|---|
| **本專案 headline（迭代+位準位移 k=6）** | 0.454†† | 0.451†† | **0.394** | — |
| **本專案 iter2(k=8)** | — | — | **0.418** | +0.024 |
| **本專案 LOSO L3 融合** | — | — | **0.457** | +0.063 |
| Mukundan & Wang 2021（TLE vs SGP4傳播差分） | 0.399 | 0.363 | 0.300 | −0.094 |
| Adaptive CuSum | 0.209 | 0.511 | 0.284 | −0.110 |
| Kelecy et al. 2007（滑窗前後多項式差分） | 0.181 | 0.667 | 0.264 | −0.130 |
| RSO Proper Elements＋BOCPD（改編代理版） | 0.511 | 0.186 | 0.223 | −0.171 |
| Peng & Bai 2018（ANN殘差修正） | 0.545 | 0.042 | 0.073 | −0.321 |

††本專案 headline 之 P/R 為 14 星（2025-05-01 切面）數字，23 星版本另計；此列僅供
F1 量級對照，詳見 `docs/App能否重現14加9星驗證數據_查核與實作_20260913.md`。

**誠實but重要的但書**：以上是本專案「用最少調參、最快驗證」精神重現的版本，**非原文
作者之原始程式碼、也未做逐星超參數搜尋**——差距不代表原始論文方法本身遜於本專案，
更可能反映：(a) 本專案的重現保真度有限（尤其 Peng & Bai 2018，見第三節說明）；
(b) 原文多數在 2-3 顆精心挑選的衛星上驗證（如 Kelecy 2007 用 TOPEX/Envisat），
本專案用 23 顆異質軌道類型衛星、零逐星調參的门槛更嚴苛。

## 二、逐方法說明與逐星結果重點

### 2.1 Kelecy et al., 2007 — 滑動視窗前後多項式(deg1)外推差分 + n-σ 門檻

實作：對每個時間點，分別用其前 w=10 點與後 10 點做線性外推至該點，兩個外推值相減
作為訊號，穩健標準差門檻 k=4。

- 平均 F1=0.264（P=0.181, R=0.667）——**全部 23 星中最高召回率的方法**，但精確率低，
  與原文獻本身「門檻設定極敏感、需視衛星調整」的已知限制一致。
- 最佳單星：Sentinel-3A F1=0.725（P=0.62, R=0.88）——資料品質好、更新頻率高的衛星
  表現接近本專案方法；最差：TOPEX/Poseidon F1=0.046——最老舊、雜訊最大的衛星，
  幾乎所有方法都在此表現不佳（見 §五）。

### 2.2 Mukundan & Wang, 2021 — TLE觀測值 vs SGP4傳播值差分

實作：對每一筆 TLE，用其前一筆 TLE 之 line1/line2 以 SGP4（`sgp4` 套件）傳播至當前
epoch，取得傳播位置/速度，用活力公式（vis-viva）反推半長軸，與當前 TLE 自身之
sma_km 相減，門檻 k=4。

- **5 篇中表現最好**：平均 F1=0.300（P=0.399, R=0.363）——精確率是 5 篇中第二高。
- 最佳單星：Sentinel-3B F1=0.713（P=0.65, R=0.79）；多顆衛星（Jason-2、GRACE-A/B）
  F1 接近 0，顯示此法對某些軌道類型（尤其低訊噪比或 TLE 更新不規則的衛星）特別敏感。

### 2.3 RSO Proper Elements（改編代理版）＋ BOCPD

**誠實聲明**：原文（*Advances in Space Research*, 2023）全文遭 ScienceDirect(403) 與
ADS(405) 阻擋，無法取得其「proper element」精確算法。本專案改用「rolling-median
去除阻力 secular 趨勢後的殘差序列」作為忠實於其研究精神（在比平均要素更乾淨的
特徵空間套用 BOCPD）之**誠實代理實作**，套用本專案既有 BOCPD（`statistical_detectors.bocpd`）。

- 平均 F1=0.223（P=0.511, R=0.186）——5 篇中精確率第二高，但召回率明顯偏低，
  可能是 rolling-median 去趨勢窗口把部分真實機動的階躍本身也一併「去除」，
  與案例十四已知的「LOWESS平滑吸收步階」問題同源。
- **此結果不能視為對原始 RSO Proper Elements 論文的公平評價**，僅代表本專案代理
  實作在同一驗證基準下的表現。

### 2.4 Adaptive CuSum（EngrXiv）

實作：把本專案既有 CUSUM（`statistical_detectors.cusum`）之全域固定門檻，改為
滑動視窗（60點）動態估計局部中位數與 MAD，其餘邏輯不變。

- 平均 F1=0.284（P=0.209, R=0.511）——**5 篇中排名第二**，且相較本專案固定版
  CUSUM 通道（既有基準 F1≈0.31-0.33，見案例十一④文獻比較）**並未帶來提升**，
  代表「自適應門檻」本身不是免費午餐，需要更細緻的視窗長度調校才能發揮效果。

### 2.5 Peng & Bai, 2018 — ANN 殘差修正

實作：用 `atmospheric_drag.py` 之物理阻力殘差（`drag_residual`）為基礎，訓練一個
簡單 MLPRegressor（僅用「時間索引」為特徵，前半段訓練、全段預測）學習修正該殘差，
再對「修正後殘差」跑同樣的穩健門檻偵測。

- **5 篇中表現最差**：平均 F1=0.073（P=0.545, R=0.042）——精確率其實不低，但召回率
  極低，7 顆衛星召回率為 0。
- **誠實根因**：本次實作**只用了時間索引一個特徵**（未如原文使用如太陽通量、
  局部時間等真實物理協變量），ANN 幾乎學不到有意義的修正模式，反而可能把部分
  真實訊號也「修正掉」——**這是本專案簡化實作的限制，不代表原文方法本身表現如此差**，
  若要公平評估，需補上真實協變量特徵重跑（列入待辦）。

## 三、與本專案 5 顆重疊衛星比較（同衛星、同定義）

| 衛星 | 本專案 headline F1 | 最佳文獻重現 F1 | 最佳文獻方法 |
|---|--:|--:|---|
| Jason-3 | 0.700†† | 0.583 | Adaptive CuSum |
| Sentinel-3A | 0.641†† | 0.725 | Kelecy 2007 |
| CryoSat-2 | 0.682†† | 0.506 | Adaptive CuSum |
| TOPEX/Poseidon | 0.092†† | 0.222 | Mukundan 2021 |
| SWOT | 0.606†† | 0.596 | Mukundan 2021 |

††引用自 `docs/App能否重現14加9星驗證數據_查核與實作_20260913.md` 之 2025-05-01
切面逐星表。**Sentinel-3A 是唯一一顆文獻重現版（Kelecy 2007）贏過本專案 headline
的衛星**（0.725 vs 0.641）——值得注意但非普遍現象，其餘多數衛星本專案 headline
領先。

## 四、第 5 篇（Geometric Distance Difference, Aerospace 2025）：暫緩說明

此方法需要 Starlink MEME 精密星曆（作為論文中「精密解」的代理）與同時段 TLE 做
幾何 RMS 距離比較。查證發現：

- `space_db.duckdb` 的 `sp_ephem_raw` 資料表（設計用途正是存放 MEME 精密星曆）
  **目前為空**（0 列）。
- 本機無任何快取之 MEME 原始檔（`starlink_ephemeris/` 套件已具備下載/解析
  基礎設施，但需即時連線 api.starlink.com 重新抓取，且需要先建立
  catalog/registry 對照檔）。
- Space-Track 已於 2025-07-28 起不再代管 MEME 鏡像，只能從 SpaceX 官方站下載。

**這是一個真實的資料缺口，非能力缺口**——`starlink_ephemeris/downloader.py` 已有
`fetch_manifest()`／`seed_from_manifest()` 等現成函式，若要補做，下一步是：
(1) 抓取即時 MANIFEST、(2) 挑 2-3 顆目前活躍 Starlink 衛星下載其 MEME、
(3) 用 `compare_tle_vs_ephemeris.py` 既有之 SGP4 傳播與 RTN 殘差計算邏輯建
「幾何距離差分」偵測器、(4) 因 MEME 僅覆蓋近期 72 小時視窗，只能驗證這幾顆衛星
「近期是否有機動」，無法比照其餘 5 篇跑 23 星歷史標竿。

## 五、共同觀察：哪些衛星對所有方法都困難

TOPEX/Poseidon、GRACE-A、GRACE-B 三顆衛星在**全部 5 篇文獻重現版與本專案方法**
中都是 F1 最低的幾顆之一——TOPEX 是最老舊衛星（1992年發射，TLE歷史雜訊特性與
現代衛星差異大），GRACE-A/B 則是全新加入的 hold-out 衛星、且軌道特性
（低軌、重力測量任務）與其餘機動較規律的測高衛星不同。這與本專案技術報告
既有發現一致：**衛星世代與軌道類型是比「換偵測方法」更主要的效能決定因素**。

## 附錄：可重現性

- 實作腳本：`lit_tier1_reproductions.py`（可用 `--quick` 跑 3 星快速測試）
- 逐星原始結果：`data/benchmark/lit_tier1_persat_20260913.csv`（115 列 = 23星 × 5法）
- 執行紀錄：`lit_tier1_run_20260913.log`
- 總耗時：276 秒（23 星 × 5 法，含 Mukundan 2021 之逐 TLE SGP4 傳播）
